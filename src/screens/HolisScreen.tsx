import React from 'react';
import { View, Text } from 'react-native';

const HolisScreen = () => {
  return (
    
        <View
      style={{
        flex: 1,
        // backgroundColor: 'red',
        justifyContent: 'center',
      }}>
      <Text
        style={{
          fontSize: 45,
          textAlign: 'center',
        }}>
        Holis :)
      </Text>
    </View>
  );
};

export default HolisScreen;